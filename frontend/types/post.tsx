// Post type used in [goalId] screen
export type Post = {
  id: string; // postId
  username: string;
  profilePhotoUrl: string;
  createdAt: string;
  updatedAt: string;
  comment: string;
  imageUrl: string;
};
